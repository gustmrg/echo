services:
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    environment:
      ConnectionStrings__DefaultConnection: "Data Source=/data/echo.db"
      AWS__S3__BucketName: "echo-files"
      AWS__S3__Region: "us-east-1"
      AWS__S3__ServiceUrl: "http://minio:9000"
      AWS__S3__AccessKey: "${MINIO_ROOT_USER}"
      AWS__S3__SecretKey: "${MINIO_ROOT_PASSWORD}"
      AWS__S3__ForcePathStyle: "true"
      RabbitMQ__HostName: "rabbitmq"
      RabbitMQ__UserName: "echo"
      RabbitMQ__Password: "echo"
    depends_on:
      minio:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    volumes:
      - appdata:/data
    networks:
      - echo-network

  worker:
    build:
      context: ./apps/worker
      dockerfile: Dockerfile
    environment:
      RABBITMQ_URL: "amqp://echo:echo@rabbitmq:5672"
      TRANSCRIPTION_QUEUE: "recording-worker"
      TRANSCRIPTION_EXCHANGE: "echo.events"
      TRANSCRIPTION_ROUTING_KEY: "recording.transcription.requested"
      RABBITMQ_PREFETCH: "1"
      RABBITMQ_REQUEUE_ON_FAILURE: "false"
      S3_BUCKET: "echo-files"
      S3_REGION: "us-east-1"
      S3_ENDPOINT: "http://minio:9000"
      S3_ACCESS_KEY: "${MINIO_ROOT_USER}"
      S3_SECRET_KEY: "${MINIO_ROOT_PASSWORD}"
      S3_FORCE_PATH_STYLE: "true"
      TRANSCRIPTION_PROVIDER: "${TRANSCRIPTION_PROVIDER:-openrouter}"
      OPENROUTER_API_KEY: "${OPENROUTER_API_KEY}"
      OPENROUTER_BASE_URL: "${OPENROUTER_BASE_URL:-https://openrouter.ai/api/v1}"
      OPENROUTER_TRANSCRIPTION_MODEL: "${OPENROUTER_TRANSCRIPTION_MODEL:-openai/gpt-4o-mini-transcribe}"
      OPENAI_API_KEY: "${OPENAI_API_KEY}"
    depends_on:
      minio-init:
        condition: service_completed_successfully
      rabbitmq:
        condition: service_healthy
    networks:
      - echo-network

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD}"
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - echo-network

  minio-init:
    image: minio/mc:RELEASE.2025-02-21T16-00-46Z
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 $${MINIO_ROOT_USER} $${MINIO_ROOT_PASSWORD} &&
      mc mb --ignore-existing local/echo-files
      "
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD}"
    networks:
      - echo-network

  rabbitmq:
    image: rabbitmq:4-management
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: echo
      RABBITMQ_DEFAULT_PASS: echo
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_running"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - echo-network

volumes:
  appdata:
  miniodata:

networks:
  echo-network:
    driver: bridge
